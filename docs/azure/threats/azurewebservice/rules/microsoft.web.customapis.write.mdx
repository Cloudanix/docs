--- 
slug: Microsoft.Web.customApis.Write
eventname: Microsoft.Web.customApis.Write
title: Microsoft.Web.customApis.Write
sidebar_label: Microsoft.Web.customApis.Write
---
                       
### Event Information

#### Meaning

1. The Microsoft.Web.customApis.Write event in Azure Realtime for Azure Web Service refers to a write operation performed on a custom API within the Azure Web Service.

2. This event indicates that data has been written or updated in a custom API endpoint within the Azure Web Service.

3. It is important to monitor this event to track changes and updates made to custom APIs, ensuring data integrity and security within the Azure Web Service.

#### Example

1. Example of security impact: If an unauthorized user gains access to the Microsoft.Web.customApis.Write event in Azure Realtime for Azure Web Service, they could potentially modify or inject malicious code into the custom APIs. This could lead to unauthorized access, data breaches, or disruption of service.

Remediation:
- Regularly review and update access control policies for the Azure Web Service to ensure that only authorized users have access to the Microsoft.Web.customApis.Write event.
- Implement strong authentication mechanisms such as multi-factor authentication (MFA) to prevent unauthorized access to the Azure Web Service.
- Monitor and analyze logs and audit trails to detect any suspicious activities related to the Microsoft.Web.customApis.Write event.

Example CLI command for updating access control policies:
```
az webapp config access-restriction add --resource-group <resource-group-name> --name <webapp-name> --rule-name <rule-name> --action Allow --ip-address <ip-address>
```

2. Example of security impact: If the Microsoft.Web.customApis.Write event is misconfigured or misused, it could potentially allow an attacker to execute arbitrary code or commands on the Azure Web Service. This could lead to unauthorized access, data breaches, or compromise of the entire Azure environment.

Remediation:
- Implement strict input validation and sanitization mechanisms to prevent code injection or command execution vulnerabilities in the custom APIs.
- Regularly update and patch the Azure Web Service to ensure that any known security vulnerabilities related to the Microsoft.Web.customApis.Write event are addressed.
- Implement network security controls such as network security groups (NSGs) or virtual network service endpoints to restrict access to the Azure Web Service.

Example CLI command for implementing network security groups:
```
az network nsg rule create --resource-group <resource-group-name> --nsg-name <nsg-name> --name <rule-name> --protocol tcp --direction inbound --source-address-prefix <source-ip-range> --source-port-range '*' --destination-address-prefix '*' --destination-port-range <webapp-port> --access allow --priority <priority>
```

3. Example of security impact: If the Microsoft.Web.customApis.Write event is exposed to the public internet without proper authentication and authorization mechanisms, it could allow unauthorized users to modify or tamper with the custom APIs. This could lead to unauthorized access, data breaches, or disruption of service.

Remediation:
- Implement authentication mechanisms such as Azure Active Directory (AAD) integration or API keys to ensure that only authorized users can access and modify the custom APIs.
- Enable role-based access control (RBAC) to restrict access to the Microsoft.Web.customApis.Write event based on user roles and permissions.
- Regularly monitor and analyze logs and audit trails to detect any suspicious activities related to the Microsoft.Web.customApis.Write event.

Example CLI command for enabling RBAC:
```
az role assignment create --assignee <user-or-group-id> --role <role-name> --scope /subscriptions/<subscription-id>/resourceGroups/<resource-group-name>/providers/Microsoft.Web/sites/<webapp-name>
```


 