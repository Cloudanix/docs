--- 
slug: DeleteHealthCheck
eventname: DeleteHealthCheck
title: DeleteHealthCheck
sidebar_label: DeleteHealthCheck
---
                       
### Event Information

#### Meaning

- The DeleteHealthCheck event in AWS Route53 refers to the deletion of a health check configuration for a specific resource or endpoint.
- This event indicates that the health check is no longer active and will no longer be used to monitor the health of the associated resource.
- It is important to note that deleting a health check does not automatically remove it from any associated DNS records or routing policies. Additional steps may be required to update the DNS configuration accordingly.

#### Example

1. Example of how security can be impacted with DeleteHealthCheck event in AWS Route53:
- If an unauthorized user gains access to the AWS account or API credentials, they can potentially delete a health check in Route53. This can lead to a disruption in the availability and reliability of the associated resources, such as DNS resolution for the domain or application.

2. Remediation for security impact caused by DeleteHealthCheck event:
- Implement strong access controls and follow the principle of least privilege to restrict access to AWS accounts and API credentials. Regularly review and revoke unnecessary permissions.
- Enable multi-factor authentication (MFA) for AWS accounts to add an extra layer of security. This helps prevent unauthorized access even if credentials are compromised.
- Enable AWS CloudTrail to capture API activity logs and monitor for any unauthorized or suspicious actions. Configure CloudTrail to send logs to an S3 bucket and enable Amazon S3 bucket versioning for added protection against accidental or malicious deletions.

3. AWS CLI command to delete a health check in Route53:
```
aws route53 delete-health-check --health-check-id <health_check_id>
```
Note: Replace `<health_check_id>` with the actual ID of the health check you want to delete. Ensure that the command is executed with appropriate permissions and access controls in place.


 